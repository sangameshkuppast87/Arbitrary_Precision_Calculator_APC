# Arbitrary_Precision_Calculator_APC
About A C-based Arbitrary Precision Calculator that performs arithmetic operations on very large numbers which cannot be handled by standard data types. This project is implemented using linked lists to store and manipulate numbers digit by digit.
